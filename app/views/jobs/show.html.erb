<h1> <%= @job.title %> </h1>

<h3><%= @job.description %></h3>
<div id="job_status">
  <% if @job.pending %>
    <p>Someone has already claimed this job; it's currently pending.</p>
    <p> This job has been claimed by the worker with the email <%= Worker.find(@job.worker_id).email %></p>
  <% else %>
  <p>No one has claimed this job!</p>
  <%= link_to "click here to claim it now", job_path(@job), :method => "patch", :id => "claim_job", remote: true %>
  <% end %>

</div>
